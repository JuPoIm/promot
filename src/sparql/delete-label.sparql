PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX obo:  <http://purl.obolibrary.org/obo/>

SELECT ?entity ?label ?def
WHERE {
  {
    ?entity rdfs:label ?label .
    FILTER(lang(?label) = "fr")
  }
  UNION
  {
    ?entity obo:IAO_0000115 ?def .
    FILTER(lang(?def) = "fr")
  }
}